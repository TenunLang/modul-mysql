impor "mysql";

biar db: bulat = mysqlSambung("127.0.0.1", 3306, "root", "");
kalau db < 0 {
    cetak("gagal login ke MySQL");
} lain {
    cetak("terhubung ke MySQL");
    biar hasil: teks = mysqlPerintah(db, "SELECT VERSION()");
    cetak(potong(hasil, 0, 80));
    tutup(db);
}
